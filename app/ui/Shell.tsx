'use client'
import Link from 'next/link'
import { useState } from 'react'
const links=[['Home','/'],['Membership','/membership'],['Digital ID','/digital-id'],['Vehicles','/vehicles'],['Complaints','/complaints'],['Welfare','/welfare'],['Government','/government'],['News','/news'],['Documents','/documents'],['Events','/events'],['Notifications','/notifications'],['Payments','/payments'],['Member Portal','/member'],['Admin ERP','/admin']]
export default function Shell({children,title='GTDOA'}:{children:React.ReactNode,title?:string}){const[open,setOpen]=useState(false);return <div><header className="site-header"><div className="container nav"><Link className="brand" href="/"><span className="logo">G</span><span>GTDOA</span></Link><button className="menu" onClick={()=>setOpen(!open)}>☰</button><nav className={open?'navlinks open':'navlinks'}>{links.map(([label,href])=><Link key={href} href={href} onClick={()=>setOpen(false)}>{label}</Link>)}</nav></div></header><main className="container page"><div className="page-heading"><div><span className="eyebrow">GTDOA PLATFORM</span><h1>{title}</h1></div><Link className="btn primary" href="/membership">Join / Apply</Link></div>{children}</main></div>}
